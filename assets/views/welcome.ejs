
<!DOCTYPE html>
<html class="min-h-screen">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta content="TechSpy is an Android Remote app which can monitor all android devices in simple way." name="description">
    <title>ParentWeb - Simple Parental Tool</title>
    <link href="https://cdn.glitch.global/d4f875f6-5b27-4ca5-90ae-cf8f62953724/favicon.png" rel="icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Mobile menu functionality
            const mobileMenu = document.getElementById('mobile-menu');
            const openMenuBtn = document.getElementById('open-menu');
            const closeMenuBtn = document.getElementById('close-menu');

            openMenuBtn.addEventListener('click', () => {
                mobileMenu.classList.remove('translate-x-full');
            });

            closeMenuBtn.addEventListener('click', () => {
                mobileMenu.classList.add('translate-x-full');
            });

            // Smooth scrolling for navigation links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                        // Close mobile menu if open
                        mobileMenu.classList.add('translate-x-full');
                    }
                });
            });

            // Scroll to top button functionality
            const scrollToTopBtn = document.getElementById('scroll-to-top');
            
            window.addEventListener('scroll', () => {
                if (window.pageYOffset > 300) {
                    scrollToTopBtn.classList.remove('opacity-0', 'pointer-events-none');
                } else {
                    scrollToTopBtn.classList.add('opacity-0', 'pointer-events-none');
                }
            });

            scrollToTopBtn.addEventListener('click', () => {
                window.scrollTo({
                    top: 0,
                    behavior: 'smooth'
                });
            });
        });
    </script>
</head>

<body class="bg-gradient-to-b from-gray-950 via-gray-900 to-gray-950 text-gray-200 min-h-screen flex flex-col">
    <!-- Mobile Menu -->
    <div id="mobile-menu" class="fixed inset-0 bg-gray-900/95 z-50 transform translate-x-full transition-transform duration-300 ease-in-out lg:hidden">
        <div class="flex justify-end p-6">
            <button id="close-menu" class="text-gray-400 hover:text-white">
                <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
        <nav class="flex flex-col items-center space-y-8 p-8">
            <a href="#hero" class="mobile-nav-link">Home</a>
            <a href="#features" class="mobile-nav-link">Features</a>
            <a href="https://links.collect.chat/5f5dec49cc65116bf72a9da5" class="mobile-nav-link">Contact</a>
        </nav>
    </div>

    <!-- Hero Section -->
    <header id="hero" class="relative min-h-screen flex items-center justify-center bg-gradient-to-r from-gray-900 to-gray-800 border-b border-gray-700/50 shadow-2xl overflow-hidden">
        <!-- Background Effects -->
        <div class="absolute inset-0 bg-[url('/img/grid.png')] opacity-10 animate-pulse"></div>
        <div class="absolute inset-0 bg-gradient-to-br from-blue-500/10 to-purple-500/10 animate-gradient"></div>
        <div class="absolute inset-0">
            <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-blue-500/5 via-transparent to-transparent animate-ripple"></div>
        </div>

        <!-- Navigation -->
        <nav class="container mx-auto px-4 fixed top-0 left-0 right-0 z-50 backdrop-blur-sm bg-gray-900/70 border-b border-gray-700/30">
            <div class="flex justify-between items-center py-6">
                <a href="/" class="flex items-center transform hover:scale-105 transition-transform duration-300">
                    <img src="https://raw.githubusercontent.com/websmartbd/ParentWeb/refs/heads/main/assets/webpublic/logo.png" alt="Logotype" class="h-10">
                </a>
                <div class="hidden lg:flex lg:items-center lg:space-x-8">
                    <a href="#hero" class="nav-link group relative overflow-hidden">
                        <span class="relative z-10">Home</span>
                        <div class="absolute inset-0 bg-gradient-to-r from-blue-500/10 to-purple-500/10 transform -translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
                    </a>
                    <a href="#features" class="nav-link group relative overflow-hidden">
                        <span class="relative z-10">Features</span>
                        <div class="absolute inset-0 bg-gradient-to-r from-blue-500/10 to-purple-500/10 transform -translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
                    </a>
                    <a href="" class="nav-link group relative overflow-hidden">
                        <span class="relative z-10">Contact</span>
                        <div class="absolute inset-0 bg-gradient-to-r from-blue-500/10 to-purple-500/10 transform -translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
                    </a>
                </div>
                <button id="open-menu" class="lg:hidden p-3 rounded-lg bg-gray-800/80 backdrop-blur-sm hover:bg-gray-700 focus:outline-none transition-all duration-300 hover:scale-105 active:scale-95">
                    <span class="sr-only">Open main menu</span>
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </nav>

        <!-- Hero Content -->
        <div class="container mx-auto px-4 relative z-10 mt-20">
            <div class="flex flex-wrap items-center justify-between">
                <div class="w-full lg:w-1/2 text-center lg:text-left mb-12 lg:mb-0">
                    <h1 class="text-5xl lg:text-7xl font-bold mb-8 bg-clip-text text-transparent bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 animate-gradient-x">Next-Gen
Monitoring Solution</h1>
                    <p class="text-gray-300 text-lg lg:text-xl mb-10 leading-relaxed max-w-2xl mx-auto lg:mx-0">Experience unparalleled device monitoring capabilities with our cutting-edge solution. Secure, efficient, and powerful.</p>
                    <div class="flex flex-col sm:flex-row justify-center lg:justify-start space-y-4 sm:space-y-0 sm:space-x-6">
                        <a href="/login" class="group relative inline-flex items-center justify-center px-8 py-4 text-lg font-medium text-white bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg overflow-hidden transition-all duration-300 transform hover:scale-105 hover:shadow-lg hover:shadow-blue-500/25">
                            <i class="fas fa-sign-in-alt mr-2"></i>
                            <span class="relative z-10">Login</span>
                            <div class="absolute inset-0 bg-gradient-to-r from-blue-700 to-purple-700 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                        </a>
                        <a href="#features" class="inline-flex items-center justify-center px-8 py-4 text-lg font-medium text-gray-300 bg-gray-800/50 backdrop-blur-sm rounded-lg hover:bg-gray-700/50 hover:text-white transition-all duration-300 transform hover:scale-105">
                            <i class="fas fa-download mr-2"></i>
                            Download
                        </a>
                    </div>
                </div>
                <div class="w-full lg:w-5/12">
                    <div class="relative">
                        <div class="absolute -inset-4 bg-gradient-to-r from-blue-500 to-purple-500 rounded-lg blur opacity-25 group-hover:opacity-40 transition duration-1000 group-hover:duration-200 animate-pulse"></div>
                        <img src="/img/hacker.png" alt="Monitoring Dashboard" class="relative rounded-lg transform hover:scale-105 transition-all duration-500 hover:shadow-2xl">
                    </div>
                </div>
            </div>
        </div>

        <!-- Scroll Indicator -->
        <a href="#features" class="absolute bottom-8 left-1/2 transform -translate-x-1/2 flex items-center justify-center space-x-2 text-gray-400 hover:text-white transition duration-300 animate-bounce">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
            </svg>
            <span>Explore Features</span>
        </a>
    </header>

    <!-- Features Section -->
    <section id="features" class="py-24 bg-gradient-to-b from-gray-900 via-gray-900/95 to-gray-800 relative overflow-hidden">
        <div class="absolute inset-0 bg-[url('/img/grid.png')] opacity-5"></div>
        <div class="container mx-auto px-4 relative z-10">
            <div class="text-center mb-16">
                <h2 class="text-4xl lg:text-5xl font-bold mb-6 bg-clip-text text-transparent bg-gradient-to-r from-blue-400 to-purple-400 animate-gradient-x">Powerful Features</h2>
                <p class="text-gray-400 text-xl max-w-3xl mx-auto leading-relaxed">Discover the comprehensive suite of tools and capabilities at your fingertips</p>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
                <% const features = [
                    { name: 'View Call Logs', icon: 'fa-solid fa-phone-volume' },
                    { name: 'View Messages', icon: 'fa-solid fa-message' },
                    { name: 'View Contacts', icon: 'fa-solid fa-address-book' },
                    { name: 'Get Location of Device', icon: 'fa-solid fa-location-dot' },
                    { name: 'File Explorer & Downloader', icon: 'fa-solid fa-folder-open' },
                    { name: 'Microphone Recording', icon: 'fa-solid fa-microphone' },
                    { name: 'View WiFi Networks', icon: 'fa-solid fa-wifi' },
                    { name: 'Live Clipboard Logging', icon: 'fa-solid fa-clipboard' },
                    { name: 'View Installed Apps', icon: 'fa-solid fa-mobile-screen' },
                    { name: 'Monitor Skype/Telegram', icon: 'fa-brands fa-telegram' },
                    { name: 'App auto-hide', icon: 'fa-solid fa-eye-slash' },
                    { name: 'Whatsapp Chats', icon: 'fa-brands fa-whatsapp' },
                    { name: 'Instagram Chats', icon: 'fa-brands fa-instagram' },
                    { name: 'Messenger Chats', icon: 'fa-brands fa-facebook-messenger' },
                    { name: 'Tinder Chats', icon: 'fa-solid fa-heart' },
                    { name: 'Bypass PlayProtect', icon: 'fa-solid fa-shield' },
                    { name: 'Undetectable by AntiVirus', icon: 'fa-solid fa-virus-slash' },
                    { name: 'Command Queuing', icon: 'fa-solid fa-terminal' },
                    { name: 'Inbuilt Apk Builder', icon: 'fa-solid fa-hammer' },
                    { name: 'Many more Features!', icon: 'fa-solid fa-ellipsis' }
                ] %>
                
                <% features.forEach(feature => { %>
                <div class="group p-6 bg-gray-800/50 backdrop-blur-sm rounded-xl shadow-lg hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 border border-gray-700/50 hover:border-blue-500/30 relative overflow-hidden">
                    <div class="absolute inset-0 bg-gradient-to-br from-blue-500/10 to-purple-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                    <div class="absolute -inset-0.5 bg-gradient-to-r from-blue-500 to-purple-500 rounded-lg blur opacity-0 group-hover:opacity-25 transition duration-500"></div>
                    <div class="flex items-center space-x-4">
                        <i class="<%= feature.icon %> text-3xl text-blue-400 group-hover:text-blue-300 transition-colors duration-300"></i>
                        <p class="text-lg font-medium text-gray-200 relative z-10 group-hover:text-white transition-colors duration-300"><%= feature.name %></p>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900 py-12 relative overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-r from-blue-500/5 to-purple-500/5"></div>
        <div class="container mx-auto px-4 relative z-10">
            <div class="flex flex-wrap justify-between items-center">
                <div class="w-full md:w-auto mb-6 md:mb-0 text-center md:text-left">
                    <p class="text-xl text-gray-300">Made With <span class="text-red-500 animate-pulse">❤️</span> By BM Shifat</p>
                </div>
                <div class="w-full md:w-auto text-center">
                    <a href="https://bmshifat.alnk.host/" class="inline-flex items-center px-6 py-3 text-lg font-medium rounded-lg text-white bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 transform hover:scale-105 transition-all duration-300 shadow-lg hover:shadow-blue-500/25">
                        Official Website
                        <svg class="ml-2 -mr-1 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                        </svg>
                    </a>
                </div>
            </div>
        </div>

        <!-- Scroll to Top Button -->
        <button id="scroll-to-top" class="fixed bottom-6 right-6 bg-gradient-to-r from-blue-600 to-purple-600 p-3 rounded-full shadow-lg hover:shadow-blue-500/25 transform hover:scale-110 transition-all duration-300 z-10 opacity-0 pointer-events-none">
            <svg class="h-6 w-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 10l7-7m0 0l7 7m-7-7v18" />
            </svg>
        </button>

        <!-- Copyright -->
        <div class="text-center text-gray-300 mt-8 relative z-10">
            CopyRight &copy; <script>document.write(new Date().getFullYear());</script>
        </div>
    </footer>

    <style>
        .nav-link {
            @apply text-gray-300 hover:text-white transition-all duration-300 px-4 py-2 rounded-lg hover:bg-gray-800/50 relative;
        }

        .mobile-nav-link {
            @apply text-gray-300 hover:text-white text-xl font-medium transition-all
        
        .animate-gradient {
            animation: gradient 8s ease infinite;
        }
        
        .animate-gradient-x {
            animation: gradientX 3s ease infinite;
        }
        
        .animate-ripple {
            animation: ripple 3s infinite;
        }
        
        @keyframes gradient {
            0% { background-position: 0%
    
    